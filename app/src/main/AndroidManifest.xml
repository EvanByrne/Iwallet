<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.evan.iwallet">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignupActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegformActivity" />
        <activity android:name=".MainhubActivity" />
        <activity android:name=".IdhubActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".BudgetActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".CardinfoActivity" />
        <activity android:name=".SecurityActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".ScanrecActivity" />
        <activity android:name=".StoreinfoActivity" />
        <activity android:name=".VouchersActivity" />
        <activity android:name=".CardK" />
        <activity android:name=".TodoActivity" />
        <activity android:name=".ImpDatesActivity">

        </activity>
    </application>

</manifest>